#!/bin/sh
# This script launches the real application executable inside the /app/pomolin directory.
# We change to the app's directory first to make sure all relative paths for libraries work correctly.
cd /app/pomolin/bin
./pomolin